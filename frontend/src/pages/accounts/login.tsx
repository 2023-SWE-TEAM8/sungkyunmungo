import { useRouter } from 'next/router'
import Layout from '@/components/layout/layout'

const Login = () => {
  const router = useRouter()

  return <Layout color={false}>Contents</Layout>
}

export default Login
